const duration = 5000;

export default function() {
  // this.transition(
  //   this.hasClass('js-show-prev'),
  //   this.use('fade', { duration }),
  // );

  // this.transition(
  //   this.hasClass('js-inline-channel-nav'),
  //   this.toValue(true),
  //   this.use('toDown', { duration }),
  //   this.reverse('toUp', { duration })
  // );

  // this.transition(
  //   this.hasClass('js-player-info'),
  //   this.toValue(true),
  //   this.use('default', { duration }),
  //   this.reverse('default', { duration })
  // );

  // this.transition(
  //   this.hasClass('js-search'),
  //   this.toValue(true),
  //   this.use('fade', { duration }),
  //   this.reverse('default', { duration: 0 })
  // );
}
