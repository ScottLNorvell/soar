import { goodResult } from 'seor/helpers/good-result';
import { module, test } from 'qunit';

module('Unit | Helper | good result');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = goodResult([42]);
  assert.ok(result);
});
