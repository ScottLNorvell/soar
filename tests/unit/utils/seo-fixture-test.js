import seoFixture from 'seor/utils/seo-fixture';
import { module, test } from 'qunit';

module('Unit | Utility | seo fixture');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = seoFixture();
  assert.ok(result);
});
